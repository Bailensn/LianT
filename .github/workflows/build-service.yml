name: Build Service

on:
  workflow_dispatch:
    inputs:
      build_mode:
        description: 'Build mode'
        type: choice
        options:
          - debug
          - release
        default: release
      linux_amd64:
        description: 'Linux amd64'
        type: boolean
        default: false
      linux_arm64:
        description: 'Linux arm64'
        type: boolean
        default: false
      windows_amd64:
        description: 'Windows amd64'
        type: boolean
        default: false
      darwin_arm64:
        description: 'Darwin arm64'
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  # ============================================================
  # Linux amd64
  # ============================================================
  linux-amd64:
    if: ${{ inputs.linux_amd64 }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: service
    env:
      BUILD_MODE: ${{ inputs.build_mode }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: '1.26.5'
          cache: true
          cache-dependency-path: service/go.sum
      - name: Build
        run: |
          if [ "$BUILD_MODE" = "release" ]; then
            CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
              go build -ldflags="-s -w" -trimpath -o LianT-Service-Linux-amd64 .
          else
            CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
              go build -o LianT-Service-Linux-amd64 .
          fi
      - uses: actions/upload-artifact@v7
        with:
          name: LianT-Service-Linux-amd64
          path: service/LianT-Service-Linux-amd64

  # ============================================================
  # Linux arm64
  # ============================================================
  linux-arm64:
    if: ${{ inputs.linux_arm64 }}
    runs-on: ubuntu-24.04-arm
    defaults:
      run:
        working-directory: service
    env:
      BUILD_MODE: ${{ inputs.build_mode }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: '1.26.5'
          cache: true
          cache-dependency-path: service/go.sum
      - name: Build
        run: |
          if [ "$BUILD_MODE" = "release" ]; then
            CGO_ENABLED=0 GOOS=linux GOARCH=arm64 \
              go build -ldflags="-s -w" -trimpath -o LianT-Service-Linux-arm64 .
          else
            CGO_ENABLED=0 GOOS=linux GOARCH=arm64 \
              go build -o LianT-Service-Linux-arm64 .
          fi
      - uses: actions/upload-artifact@v7
        with:
          name: LianT-Service-Linux-arm64
          path: service/LianT-Service-Linux-arm64

  # ============================================================
  # Windows amd64
  # ============================================================
  windows-amd64:
    if: ${{ inputs.windows_amd64 }}
    runs-on: windows-latest
    defaults:
      run:
        working-directory: service
    env:
      BUILD_MODE: ${{ inputs.build_mode }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: '1.26.5'
          cache: true
          cache-dependency-path: service/go.sum
      - name: Build
        shell: pwsh
        run: |
          if ($env:BUILD_MODE -eq "release") {
            go build -ldflags="-s -w" -trimpath -o LianT-Service-Windows-amd64.exe .
          } else {
            go build -o LianT-Service-Windows-amd64.exe .
          }
      - uses: actions/upload-artifact@v7
        with:
          name: service-windows-amd64
          path: service/LianT-Service-Windows-amd64.exe

  # ============================================================
  # macOS arm64
  # ============================================================
  darwin-arm64:
    if: ${{ inputs.darwin_arm64 }}
    runs-on: macos-latest
    defaults:
      run:
        working-directory: service
    env:
      BUILD_MODE: ${{ inputs.build_mode }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: '1.26.5'
          cache: true
          cache-dependency-path: service/go.sum
      - name: Build
        run: |
          if [ "$BUILD_MODE" = "release" ]; then
            CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 \
              go build -ldflags="-s -w" -trimpath -o LianT-Service-Darwin-arm64 .
          else
            CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 \
              go build -o LianT-Service-Darwin-arm64 .
          fi
      - uses: actions/upload-artifact@v7
        with:
          name: LianT-Service-Darwin-arm64
          path: service/LianT-Service-Darwin-arm64
